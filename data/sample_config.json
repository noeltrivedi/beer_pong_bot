{
    //IMPORTANT
    //Delete the comments when creating your config.json file
    //Otherwise, we won't be able to parse it

    "debug" : false, //prints messages to console instead of actually sending them, etc
    "use_spreadsheet" : true, //if true, make sure to save your service credentials as data/client_secret.json
    "service_credentials" : "client_secret.json", //the name of the service credentials file from Google

    "bot_id" : "get this from GroupMe",
    "listening_port" : 2200 //port doesn't really matter but make sure it's the right one for your callback url
}
